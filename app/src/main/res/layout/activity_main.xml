<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.MainActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar_main_activity"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:textAlignment="textStart"
        app:title="Phone Album"
        app:titleTextColor="#ffffff"
        android:background="#cceed0">

        <ImageButton
            android:id="@+id/ib_camera"
            android:layout_width="27dp"
            android:layout_height="27dp"
            android:layout_gravity="right|center_vertical"
            android:layout_marginEnd="20sp"
            android:background="@mipmap/camera_icon"/>

        <ImageButton
            android:id="@+id/ib_dynamic_layout2"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_gravity="right|center_vertical"
            android:layout_marginEnd="13dp"
            android:background="@mipmap/dynamc_layout2_icon"
            />

        <ImageButton
            android:id="@+id/ib_dynamic_layout"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="13dp"
            android:background="@mipmap/dynamic_layout_icon"
            android:layout_gravity="right|center_vertical"
            android:visibility="gone"/>

    </android.support.v7.widget.Toolbar>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_folder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar_main_activity"
        android:layout_alignParentStart="true"
        android:layout_marginTop="0dp">

    </android.support.v7.widget.RecyclerView>

</RelativeLayout>